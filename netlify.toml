[build]
  base = "frontend/"
  publish = "build/"
  command = "npm install && npm run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18.x"
  NPM_VERSION = "10.x"
